Drop the reversed (white) endorsed lockup here:

`Cardinal Control Systems Logo-Air Company-Reverse-RGB.svg` or `.png`

Then run:

```bash
python3 scripts/generate-sheets.py --refresh-data --self-contained
```

Or drop `cardinal-control-systems-reverse.svg` in `assets/logo-overrides/`.
